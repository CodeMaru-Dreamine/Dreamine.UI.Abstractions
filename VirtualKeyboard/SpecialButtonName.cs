namespace Dreamine.UI.Abstractions.VirtualKeyboard;

/// <summary>
/// \if KO
/// <para>가상 키보드 레이아웃의 특수 버튼 식별자 상수를 제공합니다.</para>
/// \endif
/// \if EN
/// <para>Provides identifier constants for special buttons in a virtual-keyboard layout.</para>
/// \endif
/// </summary>
public static class SpecialButtonName
{
    /// <summary>
    /// \if KO
    /// <para>언어 전환 버튼의 식별자입니다.</para>
    /// \endif
    /// \if EN
    /// <para>Identifies the language-switch button.</para>
    /// \endif
    /// </summary>
    public const string LangBtn = "_langBtn";
    /// <summary>
    /// \if KO
    /// <para>입력 모드 전환 버튼의 식별자입니다.</para>
    /// \endif
    /// \if EN
    /// <para>Identifies the input-mode switch button.</para>
    /// \endif
    /// </summary>
    public const string InputModeBtn = "_inputModeBtn";
}
